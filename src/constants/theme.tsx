interface ThemeType {
  direction: string;
  defaultRem: string;
  defaultFont: string;
}

const theme: ThemeType = {
  direction: 'ltr',
  defaultRem: '1rem',
  defaultFont: 'Open Sans',
};

export default theme;
